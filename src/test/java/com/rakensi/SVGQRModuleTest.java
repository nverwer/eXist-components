package com.rakensi;

/*
import org.exist.test.runner.XSuite;
import org.junit.BeforeClass;
import org.junit.runner.RunWith;
*/
/* X(Q)Suite testing does not work, because the default context does not have a database.
@RunWith(XSuite.class)
@XSuite.XSuiteFiles({
        "src/test/resources/com/rakensi"
})
*/

public class SVGQRModuleTest {
/*
  @BeforeClass public static void beforeClass() {
    System.out.println("BeforeClass"); // never reached
  }
*/
}
